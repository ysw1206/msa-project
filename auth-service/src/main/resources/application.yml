spring:
  application:
    name: auth-service
  security:
    oauth2:
      authorization:
        issuer-uri: http://localhost:8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8081
